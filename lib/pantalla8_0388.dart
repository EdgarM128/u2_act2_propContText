import 'package:flutter/material.dart';

class Pantalla8_0388 extends StatelessWidget {
  const Pantalla8_0388({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:
            Text("Pantalla 8 Meraz0388", style: TextStyle(color: Colors.white)),
        backgroundColor: Colors.blueAccent,
      ),
      body: Center(
        child: Column(
          children: [
            Center(
                child: Container(
              margin: const EdgeInsets.all(30),
              decoration: BoxDecoration(
                color: Color(0xff52d8f9),
                borderRadius: BorderRadius.circular(10.0),
              ),
              width: 250,
              height: 250,
              alignment: Alignment.bottomCenter,
              child: Container(
                decoration: BoxDecoration(
                  color: Color(0xff00606c),
                  borderRadius: BorderRadius.circular(10.0),
                ),
                height: 100,
              ),
            )),
            Text(
              'Double container 21308051280388',
              style: TextStyle(
                fontSize: 18,
                color: Colors.black,
              ),
            ),
          ],
        ),

      ),
    );
  } //fin widget
} //fin pagina 2
