import 'package:flutter/material.dart';

class Pantalla12_0388 extends StatelessWidget {
  const Pantalla12_0388({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Pantalla 12 Meraz0388",
              style: TextStyle(color: Colors.white)),
          backgroundColor: Colors.blueAccent,
        ),
        body: Center(
            child: Column(
          children: [
            Container(
              color: Color(0xFF94CCF9),
              padding: const EdgeInsets.all(20),
              margin: const EdgeInsets.only(left: 40, top: 40),
              child: Text(
                'Mi texto 0388',
                style: TextStyle(fontSize: 38, color: Color(0xFF04589A)),
              ),
            ),
            const Text(
              'Margin and padding 21308051280388',
              style: TextStyle(
                fontSize: 18,
                color: Colors.black,
              ),
            ),
          ],
        )));
  } //fin widget
} //fin pagina 2
