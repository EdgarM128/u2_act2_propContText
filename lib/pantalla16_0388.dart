import 'package:flutter/material.dart';

class Pantalla16_0388 extends StatelessWidget {
  const Pantalla16_0388({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Pantalla 16 Meraz0388",
              style: TextStyle(color: Colors.white)),
          backgroundColor: Colors.blueAccent,
        ),
        body: Center(
            child: Column(
          children: [
            Container(
              color: Color(0xff000000),
              child: Text(
                'Soy un texto 0388',
                style: TextStyle(fontSize: 38, color: Color(0xffffffff)),
              ),
            ),
            const Text(
              'color al widget 21308051280388',
              style: TextStyle(
                fontSize: 18,
                color: Colors.black,
              ),
            ),
          ],
        )));
  } //fin widget
} //fin pagina 2
